import React from 'react';
import { Typography } from 'antd';

const { Title } = Typography;

const ExamTaking: React.FC = () => {
  return (
    <div>
      <Title level={2}>在线考试</Title>
      <p>在线考试功能正在开发中...</p>
    </div>
  );
};

export default ExamTaking;
